version: 2.1

orbs:
  welcome: circleci/welcome-orb@0.4.1

workflows:
  test-env-vars:
    jobs:
      - build

jobs:
  build:
    docker:
      - image: circleci/node:13.8.0
        
    steps:
      - checkout
      - run:
          name: "echo an env var that is part of our project"
          command: |
            echo ${CODER_NAME} # this env var must be set within the project
